function converter() {
    let pertama = document.getElementById("celcius").value
    
    let hasil = ( parseInt(pertama) * 1.8 ) + 32 

    console.log(hasil)
}



if (celci != ''){
    fahre = (parseFloat(celci) * 1.8) + 32
} else {
    celci = (parseFloat(fahre) -32) / 1.8
} 

document.getElementById('button-convert').onclick = tempConvert
document.getElementById('button-reset').onclick = reset

function tempConvert(){
    var celci = document.getElementById("celcius").value
    var fahre = document.getElementById("fahrenheit").value
    

    fahre = (parseInt(celci) * 1.8) + 32

    document.getElementById('fahrenheit').value = parseInt(fahre)
    document.getElementById('kalkulasi').value = parseFloat(celci)
}

function reset() {
    document.getElementById('celcius').value = ''
    document.getElementById('fahrenheit').value = ''
    document.getElementById('kalkulasi').value = ''
}

// ini utama


document.getElementById('button-convert').onclick = tempConvert
document.getElementById('button-reset').onclick = reset

function tempConvert(){
    var celci = document.getElementById("celcius").value
    var fahre = document.getElementById("fahrenheit").value
    var kalkulasi = document.getElementById("kalkulasi").value

    if (celci != ''){
        fahre = (parseFloat(celci) * 1.8) + 32
    } else {
        celci = (parseFloat(fahre) -32) / 1.8
    }

    document.getElementById('celcius').value = parseFloat(celci)
    document.getElementById('fahrenheit').value = parseFloat(fahre)
}

function reset() {
    document.getElementById('celcius').value = ''
    document.getElementById('fahrenheit').value = ''
    document.getElementById('kalkulasi').value = ''
}